# Sentinel Hetzner Read-only Helper

The Hetzner read-only helper implementation is:

- `sentinel_hetzner_status_helper.py`

The full operating and sudoers-review documentation is maintained in:

- `SENTINEL_HETZNER_STATUS_HELPER.md`

This file exists under the name used by `docs/goal-ok-status.md` so the helper documentation is easy to find from either naming convention. No sudoers, systemd, environment, firewall, or fail2ban changes are applied by this repository.
